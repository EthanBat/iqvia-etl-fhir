[project]
name = "fhir-pipeline"
version = "1.0.0"
description = "File-based FHIR pipeline: NDJSON -> star-schema SQLite warehouse"
requires-python = ">=3.9"
dependencies = []  # standard library only, by design

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
